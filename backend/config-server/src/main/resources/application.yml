server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
#        native:
##          search-locations: C:/Sesac JHJ/Git/spring-cloud-msa-labs-v1/backend/config-repo
##          search-locations: file:///${user.dir}/backend/config-repo
##        git:
##          uri: file:///${user.dir}/../config-repo
#          search-locations: file:///config-repo
        git:
          uri: https://github.com/jhj010311/spring-cloud-msa-labs-v1-v1
          search-paths:
            - backend/config-repo
        health:
          repositories:
            native:
              name: native
              profiles: native
